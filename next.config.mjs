/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  output: 'standalone', // Disabled: causes EPERM on Windows; enable on Linux server

};

export default nextConfig;
